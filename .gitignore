# 환경변수
.env
*/.env
backend/.env

# AWS / Terraform (추후 사용 대비 미리 포함)
*.tfstate
*.tfstate.backup
.terraform/
terraform.tfvars
*.pem

# 운영 Helm 값은 공개 저장소에 커밋하지 않음
helm/**/values-prod.yaml
helm/**/values-*.local.yaml

# OS / 에디터
.DS_Store
.idea/
.vscode/

# 로그
*.log
